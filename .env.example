# Voice Detection UI Configuration
# Copy this file to .env.local and fill in your values.

# Recommended for public Vercel deployment:
# - Keep API secrets on server-side Vercel env vars.
# - Frontend calls /api/backend/* proxy endpoints.
VITE_USE_SERVER_PROXY=true

# Optional direct mode (local/private usage only; not recommended for public deploy)
# VITE_API_BASE_URL=https://shivam-2211-voice-detection-api.hf.space
# VITE_API_KEY=your_backend_api_key
# VITE_API_WS_BASE_URL=wss://shivam-2211-voice-detection-api.hf.space

# Optional: File/API limits
# VITE_MAX_FILE_SIZE_MB=10
# VITE_API_TIMEOUT_MS=30000

# Optional: Realtime session tuning
# VITE_REALTIME_CHUNK_DURATION_SEC=2.4
# VITE_REALTIME_STREAM_INTERVAL_MS=900
# VITE_REALTIME_TIMELINE_LIMIT=40
# VITE_REALTIME_ALERTS_LIMIT=20

# Optional: Mic recording
# VITE_MIC_CHUNK_DURATION_MS=3000
